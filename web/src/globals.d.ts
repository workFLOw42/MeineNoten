/** Zeitstempel des Builds, gesetzt in vite.config.js. */
declare const __BUILD_STAMP__: string;
/** Laufende Webversion (Anzahl Commits an web/), gesetzt in vite.config.js. */
declare const __WEB_VERSION__: string;
